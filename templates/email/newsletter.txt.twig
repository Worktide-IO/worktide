{% extends 'email/base.txt.twig' %}
{% set loc = locale|default('de') %}

{% block content %}{{ newsletterTitle }}
{{ subject }}

{{ bodyText }}

--
{{ 'email.newsletter.reason_txt'|trans({'%title%': newsletterTitle}, 'messages', loc) }}
{{ 'email.newsletter.unsubscribe'|trans({}, 'messages', loc) }}: {{ unsubscribeUrl }}{% endblock %}
