{% extends 'email/base.txt.twig' %}

{% block content %}{{ 'email.invitation.greeting'|trans({}, 'messages', locale) }}

{{ 'email.invitation.body_txt'|trans({'%workspace%': workspaceName, '%brand%': brand.name}, 'messages', locale) }}
{{ 'email.invitation.open_link_txt'|trans({}, 'messages', locale) }}

{{ acceptUrl }}

{{ 'email.invitation.valid_until'|trans({'%date%': expiresAt|date('d.m.Y')}, 'messages', locale) }}

{{ 'email.invitation.ignore'|trans({}, 'messages', locale) }}{% endblock %}
