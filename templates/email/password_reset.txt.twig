{% extends 'email/base.txt.twig' %}

{% block content %}{% if firstName %}{{ 'email.password_reset.greeting_named'|trans({'%name%': firstName}, 'messages', locale) }}{% else %}{{ 'email.password_reset.greeting'|trans({}, 'messages', locale) }}{% endif %}

{{ 'email.password_reset.body_txt'|trans({'%brand%': brand.name}, 'messages', locale) }}
{{ 'email.password_reset.open_link_txt'|trans({}, 'messages', locale) }}

{{ resetUrl }}

{{ 'email.password_reset.valid_until'|trans({'%date%': expiresAt|date('d.m.Y H:i')}, 'messages', locale) }}

{{ 'email.password_reset.ignore_txt'|trans({}, 'messages', locale) }}{% endblock %}
