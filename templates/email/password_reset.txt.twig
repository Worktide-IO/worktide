{% extends 'email/base.txt.twig' %}
{% set loc = locale|default('de') %}

{% block content %}{% if firstName %}{{ 'email.password_reset.greeting_named'|trans({'%name%': firstName}, 'messages', loc) }}{% else %}{{ 'email.password_reset.greeting'|trans({}, 'messages', loc) }}{% endif %}

{{ 'email.password_reset.body_txt'|trans({'%brand%': brand.name}, 'messages', loc) }}
{{ 'email.password_reset.open_link_txt'|trans({}, 'messages', loc) }}

{{ resetUrl }}

{{ 'email.password_reset.valid_until'|trans({'%date%': expiresAt|date('d.m.Y H:i')}, 'messages', loc) }}

{{ 'email.password_reset.ignore_txt'|trans({}, 'messages', loc) }}{% endblock %}
