{% extends 'email/base.txt.twig' %}
{% set loc = locale|default('de') %}

{% block content %}{% if firstName %}{{ 'email.portal_set_password.greeting_named'|trans({'%name%': firstName}, 'messages', loc) }}{% else %}{{ 'email.portal_set_password.greeting'|trans({}, 'messages', loc) }}{% endif %}

{% if welcomeText|default %}{{ welcomeText }}

{% endif %}{{ 'email.portal_set_password.body_txt'|trans({}, 'messages', loc) }}
{{ 'email.portal_set_password.open_link_txt'|trans({}, 'messages', loc) }}

{{ setPasswordUrl }}

{{ 'email.portal_set_password.valid_until'|trans({'%date%': expiresAt|date('d.m.Y H:i')}, 'messages', loc) }}

{{ 'email.portal_set_password.ignore_txt'|trans({}, 'messages', loc) }}{% endblock %}
