{% if firstName %}Hallo {{ firstName }},{% else %}Hallo,{% endif %}

für Sie wurde ein Zugang zum Kundenportal eingerichtet.
Bitte öffnen Sie den folgenden Link, um ein Passwort festzulegen und sich anzumelden:

{{ setPasswordUrl }}

Der Link ist bis {{ expiresAt|date('d.m.Y H:i') }} Uhr gültig und kann nur einmal verwendet werden.

Falls Sie diesen Zugang nicht erwartet haben, ignorieren Sie diese E-Mail.
