{% extends 'email/base.txt.twig' %}

{% block content %}{% if firstName %}{{ 'email.portal_set_password.greeting_named'|trans({'%name%': firstName}, 'messages', locale) }}{% else %}{{ 'email.portal_set_password.greeting'|trans({}, 'messages', locale) }}{% endif %}

{% if welcomeText|default %}{{ welcomeText }}

{% endif %}{{ 'email.portal_set_password.body_txt'|trans({}, 'messages', locale) }}
{{ 'email.portal_set_password.open_link_txt'|trans({}, 'messages', locale) }}

{{ setPasswordUrl }}

{{ 'email.portal_set_password.valid_until'|trans({'%date%': expiresAt|date('d.m.Y H:i')}, 'messages', locale) }}

{{ 'email.portal_set_password.ignore_txt'|trans({}, 'messages', locale) }}{% endblock %}
