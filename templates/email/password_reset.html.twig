{% extends 'email/base.html.twig' %}

{% set primary = brand.primaryColor %}

{% block title %}{{ 'email.password_reset.heading'|trans({}, 'messages', locale) }}{% endblock %}

{% block content %}
    <tr>
        <td style="font-size:18px; font-weight:600; padding-bottom:16px;">{{ 'email.password_reset.heading'|trans({}, 'messages', locale) }}</td>
    </tr>
    <tr>
        <td style="font-size:14px; line-height:1.6; padding-bottom:24px;">
            {% if firstName %}{{ 'email.password_reset.greeting_named'|trans({'%name%': firstName}, 'messages', locale) }}{% else %}{{ 'email.password_reset.greeting'|trans({}, 'messages', locale) }}{% endif %}<br><br>
            {{ 'email.password_reset.body'|trans({'%brand%': brand.name}, 'messages', locale) }}
        </td>
    </tr>
    <tr>
        <td align="center" style="padding-bottom:24px;">
            <a href="{{ resetUrl }}" style="display:inline-block; background:{{ primary }}; color:#ffffff; text-decoration:none; font-size:14px; font-weight:600; padding:12px 24px; border-radius:8px;">{{ 'email.password_reset.cta'|trans({}, 'messages', locale) }}</a>
        </td>
    </tr>
    <tr>
        <td style="font-size:12px; line-height:1.6; color:#71717a; padding-bottom:8px;">
            {{ 'email.password_reset.fallback_link'|trans({}, 'messages', locale) }}<br>
            <a href="{{ resetUrl }}" style="color:{{ primary }}; word-break:break-all;">{{ resetUrl }}</a>
        </td>
    </tr>
    <tr>
        <td style="font-size:12px; line-height:1.6; color:#71717a; border-top:1px solid #e4e4e7; padding-top:16px;">
            {{ 'email.password_reset.valid_until'|trans({'%date%': expiresAt|date('d.m.Y H:i')}, 'messages', locale) }}<br>
            {{ 'email.password_reset.ignore'|trans({}, 'messages', locale) }}
        </td>
    </tr>
{% endblock %}
