Projekt-Freigabe: {{ projectName }}

{{ sharingWorkspaceName }} möchte das Projekt "{{ projectName }}" in {{ brand.name }}
mit Ihrem Workspace teilen (Rolle: {{ role }}).

Melden Sie sich an und öffnen Sie diesen Link, um die Freigabe anzunehmen:
{{ acceptUrl }}

Der Link ist bis {{ expiresAt|date('d.m.Y') }} gültig.
